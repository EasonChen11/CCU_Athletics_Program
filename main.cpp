#include <iostream>

long long int fp(int x, int y, int p);

using namespace std;
int main() {
    int x,y,p;
    cin>>x>>y>>p;
    long long k=fp(x,y,p);
    cout << "this is master!" << std::endl;
    return 0;
}

long long int fp(int x, int y, int p) {
    int res=1;
    while ( )
    return res;
}
